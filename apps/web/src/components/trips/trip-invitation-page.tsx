"use client";

import { ArrowLeft, Check, Copy, LoaderCircle, Mail, Plane, Ticket, UserPlus, UsersRound } from "lucide-react";
import { useLocale, useTranslations } from "next-intl";
import { useState } from "react";
import { ErrorState, LoadingState } from "@/components/ui/data-state";
import { Link } from "@/i18n/navigation";
import { useCreateTripInvitation, useTrip } from "@/lib/query/hooks";

const INVITATION_DURATION_MS = 7 * 24 * 60 * 60 * 1_000;

export function TripInvitationPage({ tripId }: { tripId: string }) {
  const t = useTranslations("trips.workspace.invitation");
  const tWorkspace = useTranslations("trips.workspace");
  const tCommon = useTranslations("common");
  const locale = useLocale();
  const tripQuery = useTrip(tripId);
  const create = useCreateTripInvitation(tripId);
  const [email, setEmail] = useState("");
  const [inviteLink, setInviteLink] = useState<string | null>(null);
  const [copied, setCopied] = useState(false);

  if (tripQuery.isPending) return <main className="mx-auto w-full max-w-[960px] px-5 py-8"><LoadingState label={tCommon("loadingTrips")} /></main>;
  if (tripQuery.isError || !tripQuery.data) return <main className="mx-auto w-full max-w-[960px] px-5 py-8"><ErrorState error={tripQuery.error} title={t("unavailableTitle")} /></main>;
  const { trip, members, callerRole } = tripQuery.data;
  async function createInvitation() { if (!email.trim() || create.isPending) return; try { const result = await create.mutateAsync({ recipientEmail: email.trim(), expiresAt: new Date(Date.now() + INVITATION_DURATION_MS).toISOString() }); setInviteLink(`${window.location.origin}/${locale}/trips/join/${result.inviteToken}`); } catch { /* safe mutation feedback below */ } }
  async function copyLink() { if (inviteLink) { try { await navigator.clipboard.writeText(inviteLink); setCopied(true); } catch { setCopied(false); } } }
  if (callerRole !== "CREATOR") return <main className="mx-auto w-full max-w-[720px] px-5 py-8"><section role="alert" className="bg-card p-6 text-center wanderly-edge wanderly-r-lg wanderly-shadow"><h1 className="text-xl font-bold">{t("creatorOnlyTitle")}</h1><p className="mt-2 text-sm text-muted-foreground">{t("creatorOnlyBody")}</p><Link href={`/trips/${tripId}`} className="mt-5 inline-flex min-h-11 items-center gap-2 px-3 font-bold wanderly-underline"><ArrowLeft aria-hidden="true" className="size-4" />{t("back")}</Link></section></main>;

  return <main className="mx-auto w-full max-w-[960px] px-5 py-8 md:py-12 xl:max-w-[1180px]">
    <Link href={`/trips/${tripId}`} className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-muted-foreground wanderly-underline"><ArrowLeft aria-hidden="true" className="size-4" />{t("back")}</Link>
    <section className="relative mt-4 overflow-hidden bg-card wanderly-edge wanderly-r-lg wanderly-shadow xl:mx-auto xl:w-[960px] xl:origin-top xl:scale-[1.15]" aria-labelledby="member-ticket-title">
      <span aria-hidden="true" className="absolute -left-3 top-[52%] size-6 rounded-full bg-background"/><span aria-hidden="true" className="absolute -right-3 top-[52%] size-6 rounded-full bg-background"/>
      <header className="flex items-start justify-between gap-4 border-b-2 border-[var(--w-ink)] bg-[var(--w-fog)] px-5 py-4 sm:px-6"><div className="flex min-w-0 items-start gap-3"><Ticket aria-hidden="true" className="mt-1 size-5 shrink-0"/><div><p className="text-[10px] font-black uppercase tracking-[.16em] text-muted-foreground">{t("ticketStub")} · {t("eyebrow")}</p><h1 id="member-ticket-title" className="mt-1 text-2xl font-bold tracking-[-.045em]">{t("title")}</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">{t("pageBody", { trip: trip.name })}</p></div></div><Plane aria-hidden="true" className="mt-1 size-5 shrink-0 text-[var(--w-ink)]/70"/></header>
      {inviteLink ? <div className="p-5 sm:p-6" aria-live="polite"><span className="grid size-11 place-items-center bg-[var(--w-highlight)] wanderly-edge wanderly-r-sm"><Check aria-hidden="true" className="size-5" /></span><h2 className="mt-4 text-xl font-bold">{t("emailCreatedTitle", { email })}</h2><p className="mt-2 text-sm text-muted-foreground">{t("emailCreatedBody")}</p><label className="mt-5 block text-xs font-bold" htmlFor="invite-link">{t("linkLabel")}</label><input id="invite-link" readOnly value={inviteLink} className="mt-1 min-h-11 w-full rounded-[9px] border bg-background px-3 text-sm focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring/30"/><button type="button" onClick={() => void copyLink()} className="mt-3 inline-flex min-h-11 w-full items-center justify-center gap-2 px-4 text-sm font-extrabold wanderly-edge wanderly-r-sm wanderly-shadow-sm wanderly-press wanderly-action focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring/30"><Copy aria-hidden="true" className="size-4" />{copied ? t("copied") : t("copy")}</button><p className="mt-3 text-xs text-muted-foreground">{t("emailDeliveryNotice")}</p></div> : <div className="grid lg:grid-cols-[minmax(0,1fr)_260px]"><div className="p-5 sm:p-6"><h3 className="text-lg font-bold">{t("emailLabel")}</h3><p className="mt-1 text-sm leading-6 text-muted-foreground">{t("emailBody")}</p><label className="mt-5 block text-xs font-black uppercase tracking-[.1em]" htmlFor="recipient-email">{t("emailLabel")}</label><div className="relative mt-2"><Mail aria-hidden="true" className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><input id="recipient-email" type="email" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder={t("emailPlaceholder")} className="min-h-11 w-full border-2 border-[var(--w-ink)] bg-[var(--w-mist)] py-2 pl-10 pr-3 text-sm font-medium wanderly-r-sm focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring/30"/></div><p className="mt-3 text-xs leading-5 text-muted-foreground">{t("emailHint")}</p>{create.isError ? <p role="alert" className="mt-3 text-xs text-destructive">{t("createError")}</p> : null}<button type="button" onClick={() => void createInvitation()} disabled={!email.trim() || create.isPending} className="mt-5 inline-flex min-h-11 w-full items-center justify-center gap-2 px-4 text-sm font-extrabold wanderly-edge wanderly-r-sm wanderly-shadow-sm wanderly-press wanderly-action focus-visible:outline-none focus-visible:ring-4 focus-visible:ring-ring/30 disabled:cursor-not-allowed disabled:opacity-50">{create.isPending ? <LoaderCircle aria-hidden="true" className="size-4 animate-spin motion-reduce:animate-none" /> : <UserPlus aria-hidden="true" className="size-4" />}{create.isPending ? t("creating") : t("emailCreate")}</button></div><aside className="relative border-t-2 border-dashed border-[var(--w-ink)] bg-[var(--w-highlight)] p-5 lg:border-l-2 lg:border-t-0 sm:p-6"><span aria-hidden="true" className="absolute -left-3 -top-3 hidden size-6 rounded-full bg-background lg:block"/><span aria-hidden="true" className="absolute -left-3 -bottom-3 hidden size-6 rounded-full bg-background lg:block"/><div className="flex items-center gap-2"><UsersRound aria-hidden="true" className="size-5"/><div><p className="text-[10px] font-black uppercase tracking-[.16em]">{t("ticketStub")}</p><h3 className="font-bold">{t("currentMembers")}</h3></div></div><p className="mt-3 text-sm font-bold">{t("memberCount", { count: members.length })}</p><ul className="mt-4 divide-y-2 divide-[var(--w-ink)]/30 border-y-2 border-[var(--w-ink)]/30">{members.map((member) => <li key={member.userId} className="flex min-h-12 items-center justify-between gap-3 py-2 text-sm"><span className="truncate font-bold">{member.displayName}</span><span className="shrink-0 text-xs">{tWorkspace(`roleValue.${member.role}` as "roleValue.CREATOR" | "roleValue.MEMBER")}</span></li>)}</ul><p className="mt-4 text-xs leading-5">{t("memberNotice")}</p></aside></div>}
    </section>
  </main>;
}
