"use client";

import { ArrowLeft, Check, Copy, LoaderCircle, Mail, UserPlus, UsersRound } from "lucide-react";
import { useLocale, useTranslations } from "next-intl";
import { useState } from "react";
import { ErrorState, LoadingState } from "@/components/ui/data-state";
import { Link } from "@/i18n/navigation";
import { useCreateTripInvitation, useTrip } from "@/lib/query/hooks";

const INVITATION_DURATION_MS = 7 * 24 * 60 * 60 * 1_000;

export function TripInvitationPage({ tripId }: { tripId: string }) {
  const t = useTranslations("trips.workspace.invitation");
  const tCommon = useTranslations("common");
  const locale = useLocale();
  const tripQuery = useTrip(tripId);
  const create = useCreateTripInvitation(tripId);
  const [email, setEmail] = useState("");
  const [inviteLink, setInviteLink] = useState<string | null>(null);
  const [copied, setCopied] = useState(false);
  if (tripQuery.isPending) return <main className="mx-auto w-full max-w-[960px] px-5 py-8"><LoadingState label={tCommon("loadingTrips")} /></main>;
  if (tripQuery.isError || !tripQuery.data) return <main className="mx-auto w-full max-w-[960px] px-5 py-8"><ErrorState error={tripQuery.error} title={t("unavailableTitle")} /></main>;
  const { trip, members, callerRole } = tripQuery.data;
  async function createInvitation() { if (!email.trim() || create.isPending) return; try { const result = await create.mutateAsync({ recipientEmail: email.trim(), expiresAt: new Date(Date.now() + INVITATION_DURATION_MS).toISOString() }); setInviteLink(`${window.location.origin}/${locale}/trips/join/${result.inviteToken}`); } catch { /* safe mutation error below */ } }
  async function copyLink() { if (!inviteLink) return; try { await navigator.clipboard.writeText(inviteLink); setCopied(true); } catch { setCopied(false); } }
  if (callerRole !== "CREATOR") return <main className="mx-auto w-full max-w-[720px] px-5 py-8"><section role="alert" className="bg-card p-6 text-center wanderly-edge wanderly-r-lg wanderly-shadow"><h1 className="text-xl font-bold">{t("creatorOnlyTitle")}</h1><p className="mt-2 text-sm text-muted-foreground">{t("creatorOnlyBody")}</p><Link href={`/trips/${tripId}`} className="mt-5 inline-flex min-h-11 items-center gap-2 px-3 font-bold wanderly-underline"><ArrowLeft className="size-4" />{t("back")}</Link></section></main>;
  return <main className="mx-auto w-full max-w-[960px] px-5 py-8 md:py-12"><Link href={`/trips/${tripId}`} className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-muted-foreground wanderly-underline"><ArrowLeft className="size-4" />{t("back")}</Link><header className="mt-4 bg-[var(--w-fog)] px-6 py-6 wanderly-edge wanderly-r-lg wanderly-shadow"><p className="flex items-center gap-2 text-[11px] font-black uppercase tracking-[.1em] wanderly-underline"><UserPlus className="size-4" />{t("eyebrow")}</p><h1 className="mt-3 text-3xl font-bold tracking-[-.055em]">{t("title")}</h1><p className="mt-3 text-sm leading-6 text-muted-foreground">{t("pageBody", { trip: trip.name })}</p></header><div className="mt-5 grid gap-5 lg:grid-cols-[1.35fr_.65fr]"><section className="bg-card p-5 wanderly-edge wanderly-r-lg wanderly-shadow">{inviteLink ? <div aria-live="polite"><span className="grid size-11 place-items-center bg-[var(--w-highlight)] wanderly-edge wanderly-r-sm"><Check className="size-5" /></span><h2 className="mt-4 text-xl font-bold">{t("emailCreatedTitle", { email })}</h2><p className="mt-2 text-sm text-muted-foreground">{t("emailCreatedBody")}</p><label className="mt-5 block text-xs font-bold" htmlFor="invite-link">{t("linkLabel")}</label><input id="invite-link" readOnly value={inviteLink} className="mt-1 min-h-11 w-full rounded-[9px] border bg-background px-3 text-sm"/><button type="button" onClick={() => void copyLink()} className="mt-3 inline-flex min-h-11 w-full items-center justify-center gap-2 px-4 text-sm font-extrabold wanderly-edge wanderly-r-sm wanderly-shadow-sm wanderly-press wanderly-action"><Copy className="size-4" />{copied ? t("copied") : t("copy")}</button><p className="mt-3 text-xs text-muted-foreground">{t("emailDeliveryNotice")}</p></div> : <><h2 className="text-lg font-bold">{t("emailLabel")}</h2><p className="mt-1 text-sm leading-6 text-muted-foreground">{t("emailBody")}</p><label className="mt-5 block text-xs font-bold" htmlFor="recipient-email">{t("emailLabel")}</label><div className="relative mt-1"><Mail className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><input id="recipient-email" type="email" autoComplete="email" value={email} onChange={(event) => setEmail(event.target.value)} placeholder={t("emailPlaceholder")} className="min-h-11 w-full rounded-[9px] border bg-background py-2 pl-10 pr-3 text-sm"/></div><p className="mt-2 text-xs text-muted-foreground">{t("emailHint")}</p>{create.isError ? <p role="alert" className="mt-3 text-xs text-destructive">{t("createError")}</p> : null}<button type="button" onClick={() => void createInvitation()} disabled={!email.trim() || create.isPending} className="mt-5 inline-flex min-h-11 w-full items-center justify-center gap-2 px-4 text-sm font-extrabold wanderly-edge wanderly-r-sm wanderly-shadow-sm wanderly-press wanderly-action disabled:opacity-50">{create.isPending ? <LoaderCircle className="size-4 animate-spin" /> : <UserPlus className="size-4" />}{create.isPending ? t("creating") : t("emailCreate")}</button></>}</section><aside className="bg-card p-5 wanderly-edge wanderly-r-lg wanderly-shadow"><div className="flex items-center gap-2"><UsersRound className="size-5"/><h2 className="font-bold">{t("currentMembers")}</h2></div><p className="mt-1 text-sm text-muted-foreground">{t("memberCount", { count: members.length })}</p><ul className="mt-4 divide-y-2 divide-[var(--w-line)] border-y-2 border-[var(--w-line)]">{members.map((member) => <li key={member.userId} className="flex min-h-12 items-center justify-between gap-3 py-2 text-sm"><span className="truncate font-bold">{member.displayName}</span><span className="shrink-0 text-xs text-muted-foreground">{t(`roleValue.${member.role}` as "roleValue.CREATOR" | "roleValue.MEMBER")}</span></li>)}</ul><p className="mt-4 text-xs leading-5 text-muted-foreground">{t("memberNotice")}</p></aside></div></main>;
}
