services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: travelagent
      POSTGRES_PASSWORD: travelagent
      POSTGRES_DB: travelagent
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U travelagent"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    ports:
      - "3000:3000"
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: travelagent
      DB_PASSWORD: travelagent
      DB_NAME: travelagent
      NODE_ENV: production
      # OpenTelemetry — local dev points at the tempo service defined in
      # docker-compose.observability.yml. Override per environment.
      OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo:4318/v1/traces
      OTEL_SERVICE_NAME: ai-travel-agent-api
      OTEL_SDK_DISABLED: "false"
      OTEL_TRACES_SAMPLER_ARG: "1.0"
    depends_on:
      postgres:
        condition: service_healthy

  worker:
    build:
      context: ../..
      dockerfile: apps/api/Dockerfile
    command: ["node", "dist/workers/worker-main.js"]
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_USER: travelagent
      DB_PASSWORD: travelagent
      DB_NAME: travelagent
      NODE_ENV: production
      OTEL_EXPORTER_OTLP_ENDPOINT: http://tempo:4318/v1/traces
      OTEL_SERVICE_NAME: ai-travel-agent-worker
      OTEL_SDK_DISABLED: "false"
      OTEL_TRACES_SAMPLER_ARG: "1.0"
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_started

volumes:
  pgdata:
