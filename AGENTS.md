# 工程代理协作规范

本文件适用于在本仓库工作的所有 AI 编程代理（包括 Codex 与 Claude）。开始任何任务前，必须先阅读本文件，以及与目标目录相关的更具体说明；更具体的说明优先。

## 工作原则

本规范采用社区整理的、受 Andrej Karpathy 关于 AI 编程常见失误观察启发的 `CLAUDE.md` 原则：先思考、保持简单、控制改动范围、以可验证结果为目标。它不是 Andrej Karpathy 本人发布的官方规范；项目规则优先于该原则。

### 先理解，再实施

- 修改前阅读将要修改的代码、相邻模块、测试、配置和现有约定。
- 明确说明可验证的假设、风险和取舍；存在多个合理解释时，不得静默选择。会实质影响产品、架构、数据或安全的歧义必须先请求人工决定。
- 先寻找已有实现和更简单的方案，不得臆造 API、依赖、配置或项目约定。
- 将完成条件转化为可执行的验证（测试、构建、静态检查或可复现的手工验证），完成前实际运行它们。

### 简单、聚焦，但不做权宜补丁

- 只实现已确认需求所需的最少**复杂度**，避免为假设性需求预建功能或抽象。
- 改动范围应聚焦于问题根因；不要借机重写无关代码、格式化全仓库或修改无关文档。
- 修复的标准是长期可靠性，而不是 diff 最小：必须处理根因、边界条件、失败模式、兼容性和可维护性。若一个更完整但有明确边界的改动能消除系统性风险，应选择它，并说明取舍。
- 新增或修改的行为必须有覆盖其关键成功路径和失败路径的测试；不能测试时，说明原因并提供替代验证。
- 删除已停用、已被替代且不再被任何运行路径、兼容性承诺、迁移计划或文档引用的代码、配置、依赖和测试；同时删除其无效引用。不得长期保留死代码、注释掉的旧实现或“以防万一”的重复实现。若暂不能删除，必须记录保留原因、负责人和明确的移除条件/期限。

## AI Travel Agent MVP 项目约束

以下项目文件共同定义当前 MVP 的事实来源；实现不得与其冲突。发生需求、边界、验收或技术取舍变化时，必须在同一变更中更新相应文件：

- `TECH_STACK.md`：技术选型、系统边界、数据/部署决策与明确不做项；
- `docs/PRD.md`：功能需求、非目标、验收和发布标准；
- `docs/backlog.md`：交付优先级与用户故事验收条件；
- `docs/test-scenarios.md`：功能、故障与回归测试场景。

### Fixture-first 与事实边界

- 版本化 fixture 仅可用于测试和 adapter contract 验证，不得成为产品运行时或 Hero Demo 产品路径的数据源。
- live provider 超时、失败、缺少数据或结果不可信时，必须 fail closed 为 `UNAVAILABLE`；不得使用 fallback fixture 或 `Demo data`，也不得将 fixture 伪装为实时库存、报价、汇率、签证结论或真实预订结果。
- 每一项价格、路线、供应商 offer 与 visa/entry readiness 输出必须带来源及 `captured_at`/检查时间，或带 `Demo data` 标记。签证/入境能力只能输出面向个人的 readiness checklist、核验缺口和官方核验下一步；不得提供法律意见、声称获批或代办申请。

### 授权、状态与不可逆操作

- Profile、私有对话、本次输入、国籍和旅行证件资料默认私有。只有当前成员对当前行程明确授予字段级授权后，服务端才可将最小必要字段写入不可变 `constraint_snapshot`。
- `constraint_snapshot`、plan version、confirmation、`STALE` 状态、idempotency record 与 booking execution 是服务端和数据库的权威状态。前端状态、模型输出和 provider 返回值均不得直接创建、绕过或取代这些业务不变量。
- 授权撤回、授权字段变化、成员约束变化、价格/库存变化或工具结果失效时，必须使依赖它们的 plan 与 confirmations 进入 `STALE`，并在新快照下重新编排；不得静默继续使用旧结果。
- 仅当三位 required members 均对同一最新、未过期的 plan version 显式确认时，才可调用 booking orchestration sandbox。重复请求、重复或乱序 callback 必须以稳定请求 ID 幂等处理，最多产生一组参考号。
- 禁止自动扣款、真实支付、真实预订、签证申请，以及任何未经当前用户明确确认的不可逆外部操作。

### 隐私与项目级可观测性

- 国籍、旅行证件、私有 Profile 和私有对话是敏感数据。不得进入日志、指标标签、trace 属性、fixture、客户端持久状态或长上下文 prompt；仅在已授权且完成当前服务端操作确有必要时，以最小字段集处理。
- 必须为授权授予/撤回、snapshot 创建、plan 失效/replan、provider fallback、确认/拒绝、sandbox 请求及重复/乱序 callback 记录安全的审计事件和可关联遥测。使用 `trip_id`、`plan_version`、`run_id`、`orchestration_request_id` 等关联标识时，只能放在 trace/log 上下文中，不能作为指标标签。

### 测试与技术栈收敛

- 任何影响功能行为、授权、状态转换、provider fallback 或用户可见事实的变更，必须同步更新 `docs/test-scenarios.md`，并覆盖适用的未授权访问、授权撤回、数据缺失、live API fallback、plan 过期、成员拒绝确认、重复 callback 与乱序 callback。
- 以 `TECH_STACK.md` 的 Hackathon MVP 边界为准。未经先行更新相关产品和技术文档并说明取舍，不得引入自由多 Agent、Redis、Temporal、Step Functions、WebSocket、真实支付、额外旅行数据 API 或全局客户端业务真相状态。

## 面向对象设计

适用于以对象模型为主的生产代码。设计和评审时应明确体现以下原则：

- **封装**：隐藏内部状态和实现细节，通过稳定、受约束的接口维护不变量；避免随意暴露可变状态。
- **抽象**：由业务能力定义接口和边界，依赖抽象而非具体实现；抽象必须服务当前真实变化点。
- **继承**：仅在确有稳定的“is-a”关系及可替换性时使用；优先组合、委托和接口，避免脆弱基类与仅为复用代码的继承。
- **多态**：通过明确的契约实现可替换行为；子类型必须满足父类型的语义、错误处理和性能预期。

不要为了形式而把脚本、数据转换、函数式代码或语言不支持的场景强行对象化。此类例外应在变更说明中简述理由。

## 可观测性工程

所有面向生产的服务、API、后台任务、异步流程和关键业务操作，都必须在设计时纳入 Metrics、Logs 和 Traces；可观测性是功能的一部分，而非故障后的补丁。

- **Metrics**：为吞吐、延迟、错误和关键业务结果提供低基数指标；定义名称、单位、标签、SLO/告警的归属，避免把用户 ID、请求 ID、URL 参数等高基数字段作为标签。
- **Logs**：使用结构化日志，包含足以诊断的上下文（如组件、操作、结果、错误码和关联 ID）；日志不得包含密码、令牌、Cookie、个人敏感数据或未脱敏请求体。
- **Traces**：跨入站请求、后台任务、数据库和外部调用传播 trace context；关键边界创建有意义的 span，并记录安全、低基数的属性与错误状态。
- 三者需能关联：日志携带 trace/span 或相关 ID，指标按稳定维度聚合，仪表化不得显著破坏性能或可用性。
- 纯本地工具或低风险一次性脚本至少提供清晰的错误信息和结构化/可解析的运行摘要；是否需要完整三件套按运行环境和影响比例决定，并记录理由。

## 文档与配置契约

- 每项开发、修复或排查工作都必须新增或更新与其功能相对应的文档。文档应位于项目约定的位置，说明目的、行为/接口、配置、运行与验证方式，以及已知限制或排查结论；避免重复代码本身显而易见的内容。
- 仅在临时调查且没有持久结论时，可在交付记录中说明无需新增文档的理由。任何会影响使用者、运维、接口消费者或后续排障的结论都必须沉淀为仓库文档。
- 新增或修改 `.env`、`.env.*`、环境变量、密钥引用或其他本地配置时，必须同步新增或更新对应的安全示例文件（如 `.env.example`）。示例文件须包含变量名、无敏感值的占位符、用途、格式、必填性与安全说明。
- `.env.example`、文档、日志、测试夹具和提交历史中不得写入真实密码、令牌、私钥、Cookie、连接串中的凭据或个人敏感数据。示例文件不是秘密存储位置。

## Git 与发布治理（强制）

- `main` 是人工控制分支。AI 代理不得在 `main` 上创建、修改、提交、合并、推送、重置、变基或执行任何其他写操作；对 `main` 的合并、推送和发布只能由人完成。
- `develop` 是唯一集成目标。所有 AI 代理创建的拉取请求或合并请求必须以 `develop` 为目标；不得以 `main` 为目标。
- 不直接在 `develop` 提交。代理应在从 `develop` 创建的短生命周期功能/修复分支上工作，经审查与 CI 通过后，合并到 `develop`。如果项目尚未存在 `develop`，必须由人工创建并确认后，代理才可开始需要提交的工作。
- 严禁对 `main` 或 `develop` 使用强制推送、强制更新、破坏性重写历史、重置或变基。代理也不得绕过分支保护、审查、CI 或 required checks。
- 在执行任何 Git 写操作前，先检查当前分支、工作区状态和目标分支；保留用户已有的未提交改动。未经明确授权，代理不得提交、推送、合并、创建 PR 或修改分支保护。

## 交付标准

- 报告修改内容、关键设计决定、实际运行的验证及其结果；未运行或失败的验证必须明确列出。
- 说明新增的配置、迁移、运行依赖、可观测性信号及回滚/兼容性影响（如有）。
- 不声称完成未验证的工作；不以“最小改动”掩盖长期风险，也不以“长期修复”为由扩大到无关范围。
